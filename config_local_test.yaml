# Local test configuration
# Run with: python -m speechlab_diarization.main --config config_local_test.yaml

models:
  pyannote_pipeline: "pyannote/speaker-diarization-3.1"

voice_type:
  backend: "vtc2"
  vtc2_root: "/opt/vtc2"

huggingface:
  token_env_var: "HF_TOKEN"

runtime:
  sample_rate: 16000
  device: "mps"
  max_duration_minutes: 60

# for apptainer purposes
io:
  input_dir: "/data/input"
  output_dir: "/data/output"
  # input_dir: "/Users/gracelee/Desktop/Speech-Lab/Speechlab/test_input"
  # output_dir: "/Users/gracelee/Desktop/Speech-Lab/Speechlab/hypo_files"