models:
  pyannote_pipeline: "pyannote/speaker-diarization-3.1"

voice_type:
  backend: "vtc2"
  vtc2_root: "/opt/vtc20"
  vtc2_checkpoint: "VTC-2.0/model/best.ckpt"
  vtc2_config: "VTC-2.0/model/config.yml"

huggingface:
  token_env_var: "HF_TOKEN"

runtime:
  sample_rate: 16000
  device: "cuda"
  max_duration_minutes: 60

io:
  input_dir: "/data/input"
  output_dir: "/data/output"
